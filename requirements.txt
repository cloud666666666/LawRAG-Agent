# Hugging Face 模型下载相关依赖
transformers>=4.30.0
huggingface_hub>=0.16.0
torch>=1.13.0
requests>=2.28.0
tqdm>=4.64.0

# 可选依赖（用于特定模型）
# accelerate>=0.20.0  # 用于大模型加速
# safetensors>=0.3.0  # 用于安全张量格式
# sentencepiece>=0.1.99  # 用于某些分词器
# protobuf>=3.20.0  # 用于某些模型配置

# PDF处理相关依赖
PyPDF2>=3.0.0
pdfplumber>=0.9.0
